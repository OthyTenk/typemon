{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../web && pnpm prisma generate && next build",
  "outputDirectory": "dist",
  "framework": "nextjs",
  "rewrites": [{ "source": "/:path*", "destination": "/" }]
}